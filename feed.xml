<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://jac2125.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jac2125.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-27T12:59:51+00:00</updated><id>https://jac2125.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">When the t-stat Was Too Good to Be True</title><link href="https://jac2125.github.io/blog/2026/when-the-t-stat-was-too-good/" rel="alternate" type="text/html" title="When the t-stat Was Too Good to Be True"/><published>2026-05-27T00:00:00+00:00</published><updated>2026-05-27T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2026/when-the-t-stat-was-too-good</id><content type="html" xml:base="https://jac2125.github.io/blog/2026/when-the-t-stat-was-too-good/"><![CDATA[<p>I bought some stock right before leaving for the military. It dropped while I was away, and there is a particular kind of helplessness in watching a number fall when you cannot touch it. To redirect that ache I started studying optimization — and somewhere in there I realized optimization is not just a math exercise, it reaches into markets too. That realization pulled me into quant, and the first thing that caught my eye was a phenomenon called <strong>PEAD</strong>: Post-Earnings-Announcement Drift. The claim is simple and almost too tidy — after a company beats earnings expectations, its price keeps drifting upward for weeks, as if the market digests good news slowly. If that drift is real and measurable, it is an optimization problem in disguise: <em>which combination of variables best explains the abnormal return?</em></p> <p>So I wrote code to validate it. My first hypothesis was deliberately naive — the plainest possible version of the claim: <strong>if the EPS surprise is positive, then the cumulative abnormal return over the next 30 days (t+1 to t+30) will be greater than zero.</strong> That sentence is testable, which is its virtue; it is also hiding two traps that took the rest of this journey to surface. (The full original script is in the appendix.)</p> <p>And the first time I ran the significance test, I got t-statistics like ( \pm 12 ) and ( \pm 23 ). I should have been thrilled. Instead something felt wrong. A t-stat that large is not a discovery; it is a smell. This post is about chasing that smell down to its source, rebuilding the test correctly, and then — honestly — finding no signal at all, and why that “failure” is the most useful result I have gotten so far.</p> <h2 id="why-the-original-test-was-broken">Why the original test was broken</h2> <p>My original code did something that looks reasonable until you stare at it. For each earnings event, it computed a 30-day path of cumulative abnormal returns (CAR), then ran a one-sample t-test on that path:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ar</span> <span class="o">=</span> <span class="n">event_window</span><span class="p">[</span><span class="sh">'</span><span class="s">Stock</span><span class="sh">'</span><span class="p">]</span> <span class="o">-</span> <span class="n">expected_return</span>   <span class="c1"># 30 daily abnormal returns
</span><span class="n">car</span> <span class="o">=</span> <span class="n">ar</span><span class="p">.</span><span class="nf">cumsum</span><span class="p">()</span>                               <span class="c1"># 30 cumulative values
</span><span class="n">t_stat</span><span class="p">,</span> <span class="n">p_value</span> <span class="o">=</span> <span class="nf">ttest_1samp</span><span class="p">(</span><span class="n">car</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>           <span class="c1"># the bug lives here
</span></code></pre></div></div> <p>The problem is what <code class="language-plaintext highlighter-rouge">ttest_1samp</code> <em>assumes</em> about its input. The t-statistic is</p> \[t = \frac{\bar{x} - \mu_0}{s / \sqrt{n}}\] <p>and the entire formula rests on one quiet assumption: that the sample standard deviation ( s ) correctly measures how much the sample mean wobbles. That only holds if the observations are <strong>independent</strong>. A CAR path violates this as badly as a series possibly can, because ( \text{CAR}<em>t = \text{CAR}</em>{t-1} + r_t ) — each value is the previous value plus one small increment. Thirty consecutive points share twenty-nine of their thirty summands. They are not “somewhat correlated”; they are almost perfectly correlated.</p> <blockquote> <p><strong>The Hidden Math: why dependence inflates the t-stat</strong> Start from how variance behaves under addition. For two random variables, \(\text{Var}(X + Y) = \text{Var}(X) + 2\,\text{Cov}(X, Y) + \text{Var}(Y).\) When ( X ) and ( Y ) are independent, ( \text{Cov}(X, Y) = 0 ) and the variances simply add. The t-test is built on exactly this clean addition. But a cumulative sum has enormous positive covariance between neighbors, so the true variance of the path is far larger than the independent-case formula assumes. The test plugs in ( s / \sqrt{n} ) as if the covariance terms were zero — it <em>underestimates the standard error severely</em>. A too-small denominator produces a monstrously large ( t ). That is exactly where ( \pm 12 ) and ( \pm 23 ) came from: not a strong effect, but a denominator computed under an assumption that was violated.</p> </blockquote> <p>The deeper lesson is that the original test was not testing PEAD at all. It was asking, thirty dependent ways, “did this one stock drift?” — a question about a single path, not about a phenomenon.</p> <h2 id="the-real-unit-of-analysis">The real unit of analysis</h2> <p>PEAD is not a claim about one stock. It is a <strong>cross-event</strong> claim: <em>across many positive-surprise events, the average drift is greater than zero.</em> Once you say it that way, the fix becomes obvious — and it is not a fix to the test, it is a fix to what counts as an observation.</p> <blockquote> <p><strong>Unit of Analysis: the quietest decision in any test</strong> Every statistical test silently answers “what is one observation?” before it does anything else. My broken code answered <em>“one observation = one day,”</em> so a single event contributed thirty dependent points and the test collapsed. The correct answer is <em>“one observation = one event.”</em> Then each event contributes exactly <strong>one number</strong> — the terminal cumulative abnormal return ( \text{CAR}_{t+30} ), which summarizes the whole 30-day drift. I am not trying to <em>remove</em> the dependence among the thirty within-event points; I am declining to use them as my sample at all. The dependence does not get smaller — it becomes <em>irrelevant</em>, because the independence I now need lives between <em>different</em> events (Apple’s Q2, Microsoft’s Q1, and so on), which do not share partial sums.</p> </blockquote> <p>With that, the rebuild is two layered changes. First, <strong>what</strong> I test: collect one terminal CAR per event into a cross-event sample, instead of testing a per-stock path. Second, <strong>which</strong> test: a one-sample, one-sided test against zero, because PEAD predicts a <em>direction</em> (upward), not merely “different from zero”:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">t_stat</span><span class="p">,</span> <span class="n">p_value</span> <span class="o">=</span> <span class="nf">ttest_1samp</span><span class="p">(</span><span class="n">terminal_cars</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">alternative</span><span class="o">=</span><span class="sh">'</span><span class="s">greater</span><span class="sh">'</span><span class="p">)</span>
</code></pre></div></div> <table> <tbody> <tr> <td>The <code class="language-plaintext highlighter-rouge">alternative='greater'</code> handles the one-sided question directly, retiring the awkward manual <code class="language-plaintext highlighter-rouge">p_value / 2</code> from the old code. Sanity check: the t-stat dropped from ( \pm 23 ) down to about ( -1.16 ). The bug was genuinely gone. In a large sample, a small (</td> <td>t</td> <td>) does not mean “I failed to see an effect” — it means “I looked, with plenty of data, and there is none.” That distinction set up everything that followed.</td> </tr> </tbody> </table> <h2 id="the-diagnostic-journey">The diagnostic journey</h2> <p>A negative t-stat with ( N = 1060 ) events is not a green light to declare “PEAD runs backward.” It is an invitation to ask <em>why</em>. What follows is a chain of hypotheses, most of which the data killed — and the killing is the point.</p> <p>The first three numbers already told a story: mean terminal CAR ( -0.0020 ), but <strong>median</strong> ( +0.0009 ), with only ( 51\% ) of events positive. A mean and median of opposite sign means a left-skewed distribution: most events sit near zero, while a few extreme negatives drag the mean down. So the negative sign was never evidence of reverse drift; it was contamination from a minority of events.</p> <p>Bucketing events by surprise magnitude found the contamination precisely. The strongest-surprise bucket showed a surprise <em>range</em> up to <strong>1671%</strong> — which is impossible as a real earnings beat and only happens when the denominator (expected EPS) is near zero. An estimate of $0.01 against an actual of $0.18 explodes the ratio. That bucket was not “great earnings”; it was a set of events where the surprise <em>metric itself</em> had broken. Capping surprise at a sane threshold pulled the mean back to roughly zero and flipped the t-stat positive — but only to ( +0.09 ). Removing contamination explained why the mean was <em>negative</em>; it did not produce a <em>signal</em>.</p> <p>That left two suspects. One was the surprise definition — but the raw data carried both <code class="language-plaintext highlighter-rouge">EPS Estimate</code> and <code class="language-plaintext highlighter-rouge">Reported EPS</code>, confirming the surprise was measured against analyst consensus, which is exactly right for PEAD. Suspect dismissed. The other was <strong>event timing</strong>: announcements released after the market close are first tradable the <em>next</em> session, so aligning the window to the calendar date can be off by a day. Inspecting the timestamps showed announcements clustered in two groups — after-hours <em>and</em> pre-market — meaning any fixed “+1 day” rule would misalign the two groups in <em>opposite</em> directions, plausibly cancelling the signal in the average.</p> <p>This suspect felt strong, and the tempting move was to write the fiddly timezone-and-trading-calendar code to fix it. Instead I drew the <strong>average CAR path</strong> first — the most diagnostic picture in PEAD research — by averaging across events at each day ( t+1, \dots, t+30 ). The verdict was decisive <em>against</em> my own hypothesis:</p> <table> <thead> <tr> <th>day</th> <th>mean CAR</th> </tr> </thead> <tbody> <tr> <td>t+1</td> <td>( -0.0018 )</td> </tr> <tr> <td>t+5</td> <td>( -0.0010 )</td> </tr> <tr> <td>t+10</td> <td>( -0.0005 )</td> </tr> <tr> <td>t+20</td> <td>( -0.0015 )</td> </tr> <tr> <td>t+30</td> <td>( -0.0038 )</td> </tr> </tbody> </table> <p>If timing misalignment were the culprit, the first reaction day’s large jump would sit inside the window and the path would show a visible step near ( t+1 ). It does not. The path is flat from day one — a narrow negative band that never lifts. <strong>Timing was not the culprit.</strong> Drawing the picture before doing the surgery saved me from writing careful code in service of a wrong theory.</p> <blockquote> <p><strong>Mathematical Nuance: what flatness rules out</strong> The signal-to-noise ratio is the whole story. The mean terminal CAR is about ( -0.2\% ) while its standard deviation across events is ( 5.5\% ). The signal is roughly ( 0.036 ) of the noise. With ( N \approx 1000 ), the ( \sqrt{n} ) in the denominator is a magnifying glass — a real effect of even modest size would surface as a respectable ( t ). It does not surface. That is not “underpowered”; it is “powered, and empty.”</p> </blockquote> <p>The honest conclusion is unglamorous: <strong>in this sample, the PEAD drift is simply absent.</strong> And that actually agrees with theory once you look at the universe — NVIDIA, Alphabet, Apple, Microsoft, Amazon. These are the most-watched megacaps on earth, where thousands of analysts price in information instantly. PEAD has always been strongest in <em>small, lightly-covered, less-liquid</em> stocks. Finding no drift in megacaps is not a contradiction of the theory; it is a confirmation of where the theory says drift should <em>not</em> be.</p> <h2 id="how-i-would-extend-this">How I would extend this</h2> <p>The path forward almost chooses itself: <strong>swap the universe for smaller-cap stocks</strong> and rerun the identical pipeline. I am choosing this over a more ambitious move — piling on extra conditioning variables (prior-quarter EPS trends, market regime, and so on) — for a deliberate reason. Changing only the cap size is a <em>controlled experiment</em>: if the signal reappears, “megacap efficiency” is confirmed as the explanation, because nothing else moved. Adding five variables at once would leave me unable to say which one mattered if the signal showed up — or stayed hidden.</p> <p>And there is a trap waiting precisely here, the one I could feel before I could name it: the urge to keep changing variables <em>until a signal appears.</em> This is dangerous not because it wastes time but because it manufactures false signals. At the ( 5\% ) level, one in twenty tests is significant by pure chance, so sweeping twenty variable combinations will hand me a “discovery” that is nothing but noise — and a person who <em>wants</em> a signal will stop exactly there and believe it. That is p-hacking, and it is the single biggest reason empirical finance fails to replicate. The defense is counterintuitive: not searching faster, but committing to a rule <em>before</em> I look. Write the hypothesis and its falsification condition first; accept the one result; and when I eventually do sweep many variables, correct for multiplicity (Bonferroni, or controlling the false discovery rate) so that twenty attempts face a stricter bar than one.</p> <p>When I left for the military, the market moved without me and I could only watch. What I am building now is the opposite posture — not predicting where a price goes, but learning to ask a question so carefully that the answer, even when it is “no,” is one I can trust. The first honest “no” is already worth more than the ( \pm 23 ) that started all this.</p> <hr/> <h2 id="appendix-the-original-script">Appendix: the original script</h2> <p>This is my first attempt, exactly as written — including the two flaws this post dissects: the per-event <code class="language-plaintext highlighter-rouge">ttest_1samp(car, 0)</code> on a cumulative-sum path (line 72), and the dangling <code class="language-plaintext highlighter-rouge">ttest_ind</code> on <code class="language-plaintext highlighter-rouge">CAR_Mean</code> that sat after the CSV write and was never reported (line 94). I am keeping it unedited as the honest starting point.</p> <pre><code class="language-ptyhon">import yfinance as yf
import pandas as pd
import statsmodels.api as sm
from scipy.stats import ttest_1samp
import numpy as np

# 1. Load data and set parameters
earn_call_raw = pd.read_csv("earn_cal_df.csv")
earn_call = earn_call_raw[earn_call_raw["Surprise(%)"] &gt; 0].reset_index(drop=True)  # positive surprises only
market_ticker = "^GSPC"   # S&amp;P 500
t_days = 30               # holding window length (trading days)
estimation_days = 250     # market-model estimation window (trading days)
buffer_days = 20          # gap between estimation window and the event
surprise_cap = 100        # drop denominator-explosion artifacts (expected EPS near zero)

# Cross-event sample: one terminal CAR per event
event_terminal_cars = []

# 2. Per-event loop
for idx, row in earn_call.iterrows():
    ticker = row["Symbol"]
    surprise = row["Surprise(%)"]

    # Skip surprise outliers: huge values come from a near-zero EPS denominator,
    # not from a genuinely large beat.
    if surprise &gt; surprise_cap:
        continue

    event_date_str = row["Event Start Date"][:10]   # keep the calendar date only
    event_date = pd.to_datetime(event_date_str)

    # Download a generous price window around the event
    start_date = event_date - pd.DateOffset(days=400)
    end_date = event_date + pd.DateOffset(days=50)

    try:
        data = yf.download([ticker, market_ticker], start=start_date, end=end_date)["Close"]
        returns = data.pct_change().dropna()
        returns.columns = ["Market", "Stock"]

        # Locate the event date among trading days
        try:
            event_idx = returns.index.get_loc(event_date)
        except KeyError:
            print(f"{ticker}: {event_date_str} is not a trading day. Skipping.")
            continue

        # Estimation window: from 250 days before to 20 days before the event
        est_start = max(0, event_idx - estimation_days)
        est_end = max(0, event_idx - buffer_days)
        estimation_window = returns.iloc[est_start:est_end]

        if len(estimation_window) &lt; 50:
            print(f"{ticker}: not enough estimation data. Skipping.")
            continue

        # Event window: [t+1, t+30] — the day AFTER the announcement through t+30.
        # The announcement-day jump is the immediate reaction, not drift, so it is excluded.
        event_start = event_idx + 1
        event_end = event_idx + 1 + t_days
        event_window = returns.iloc[event_start:event_end]

        if len(event_window) &lt; t_days:
            print(f"{ticker}: not enough event data. Skipping.")
            continue

        # Market-model OLS: Stock ~ alpha + beta * Market, fit on the estimation window
        X_est = sm.add_constant(estimation_window["Market"])
        y_est = estimation_window["Stock"]
        model = sm.OLS(y_est, X_est).fit()

        # Abnormal returns over the event window, then cumulative AR
        X_event = sm.add_constant(event_window["Market"])
        expected_return = model.predict(X_event)
        ar = event_window["Stock"] - expected_return
        car = ar.cumsum()

        # The per-event statistic is the TERMINAL CAR (total 30-day drift),
        # one number per event — this is the cross-event sample unit.
        event_terminal_cars.append(car.iloc[-1])

    except Exception as e:
        print(f"{ticker}: error during processing: {e}")
        continue

# 3. Cross-event test
# PEAD as a phenomenon: is the AVERAGE terminal CAR across events &gt; 0?
# One-sample (vs a fixed 0), one-sided (greater) — matching the directional prediction.
car_array = np.array(event_terminal_cars)
t_stat, p_value = ttest_1samp(car_array, 0, alternative="greater")

print(f"N events          = {len(car_array)}")
print(f"mean terminal CAR = {car_array.mean():.4f}")
print(f"t-statistic       = {t_stat:.4f}")
print(f"p-value (&gt;0)      = {p_value:.4f}")
</code></pre>]]></content><author><name></name></author><category term="quant"/><category term="quant"/><category term="statistics"/><category term="PEAD"/><category term="hypothesis-testing"/><summary type="html"><![CDATA[Rebuilding a broken PEAD test from the ground up — why a cumulative sum breaks a t-test, what the real unit of analysis is, and how a diagnostic journey ended in an honest "no signal here (yet)."]]></summary></entry><entry><title type="html">Reading Your Own Mail: What XHR Interception Actually Is</title><link href="https://jac2125.github.io/blog/2026/XHR-and-XHR-Intercepts/" rel="alternate" type="text/html" title="Reading Your Own Mail: What XHR Interception Actually Is"/><published>2026-05-26T00:00:00+00:00</published><updated>2026-05-26T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2026/XHR-and-XHR-Intercepts</id><content type="html" xml:base="https://jac2125.github.io/blog/2026/XHR-and-XHR-Intercepts/"><![CDATA[<p>I was refreshing a page for the hundredth time when a dumb little question stopped me: <em>where does all this data actually come from?</em> The headlines, the timestamps, the little list that re-renders when I type—none of it arrived with the page. It showed up afterward, quietly, from somewhere. And then the question that actually started this whole thing: when a letter arrives in my mailbox, I just open it. Could I receive <em>this</em> data the same way—directly, before the page dresses it up for me?</p> <p>That question turns out to have a precise, well-mapped answer in network and security theory. This post is me walking through it, as a math student who knew algorithms and Linux tools but had never once opened the Network tab. If that’s you too, good. Let’s open the mail.</p> <h2 id="the-setup-a-page-is-really-two-systems-pretending-to-be-one">The setup: a page is really two systems pretending to be one</h2> <p>Here’s the first thing that reorganized my mental model. The modern web page you look at is not one program. It’s <strong>two</strong>:</p> <ul> <li>a <strong>frontend</strong>: the HTML/CSS/JavaScript that paints pixels, and</li> <li>a <strong>backend API</strong>: a separate service whose entire job is to hand out structured data (usually JSON).</li> </ul> <p>When you hit “search,” the frontend doesn’t compute the results. It fires a background request to the API, receives a blob of JSON, and <em>then</em> renders it into the tidy list you see. The pretty page is the last step, not the source.</p> <p>To play with this safely, I built a tiny version of exactly that shape on my own VM—a FastAPI backend exposing one endpoint, <code class="language-plaintext highlighter-rouge">/api/articles-by-search</code>, and a one-page frontend that calls it. Everything below is run against <em>my own</em> server, which matters more than it sounds like. We’ll get to why.</p> <h2 id="xhr-fetch-and-interception">XHR, Fetch, and “interception”</h2> <p><strong>Definition.</strong> <strong>XHR</strong> stands for <code class="language-plaintext highlighter-rouge">XMLHttpRequest</code>—a browser mechanism, dating to the early 2000s, that lets a page exchange data with a server <em>in the background, without reloading the whole page</em>. Its modern successor is the <code class="language-plaintext highlighter-rouge">fetch</code> API. Together they’re the machinery behind AJAX and every Single Page Application (SPA). In your browser’s DevTools, the <code class="language-plaintext highlighter-rouge">Fetch/XHR</code> filter in the Network tab is showing you exactly these requests.</p> <p><strong>Intuition.</strong> Before XHR, clicking a link meant the entire page went blank and reloaded. XHR is what made “the list updates but the page doesn’t flicker” possible. It split the <em>conversation with the server</em> away from the <em>repainting of the screen</em>.</p> <p><strong>XHR interception</strong>, then, is the act of stepping into that background conversation—observing or altering the request/response before the page gets to dress it up.</p> <blockquote> <p><strong>Design Trade-off — why split the page into two systems at all?</strong> Coupling rendering and data-fetching into one server-rendered page is simpler to reason about, so why did the web move to this two-system SPA design? The trade-off buys you three things: the backend becomes a reusable data source (the same API can feed a website, a mobile app, and my little script equally), the frontend becomes responsive (update a fragment, not the world), and the two halves can be developed and scaled independently. The cost you pay is precisely the thing this post is about: a clean, observable seam opens up between them. The JSON now travels across a boundary you can stand inside. Good architecture and “interceptable” are the <em>same</em> property viewed from two sides.</p> </blockquote> <h2 id="the-core-picture-the-clientserver-trust-boundary">The core picture: the client–server trust boundary</h2> <p>Almost all of network security reasoning starts from one diagram:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[ client ] &lt;------ network ------&gt; [ server ]
 (browser)     (this part is hostile)   (API backend)
</code></pre></div></div> <p>The founding assumption is blunt: <strong>the network in the middle is untrusted.</strong> Between leaving the client and reaching the server, data passes through routers, ISPs, that café Wi-Fi access point, proxies—any of which <em>could</em> be watching or interfering. You design as if someone is.</p> <p>Standing in that middle position is what “interception” means in the security sense, and it comes in two flavors:</p> <ul> <li><strong>Passive interception (eavesdropping):</strong> you look, you don’t alter. The flow continues unchanged.</li> <li><strong>Active interception (tampering):</strong> you modify, inject, or block.</li> </ul> <p>That distinction isn’t academic—it’s why there are two <em>different</em> defenses later: you stop eavesdropping with <strong>encryption</strong>, and you stop tampering with <strong>integrity checks</strong>.</p> <h2 id="where-interception-happens-decides-what-it-means">Where interception happens decides what it <em>means</em></h2> <p>This is the idea I most want to plant, because it’s the one that finally made the legal/ethical line make sense to me—not as a rule someone imposed, but as a consequence of the structure.</p> <p>The <em>same technique</em> has completely different meaning depending on <strong>where</strong> you stand:</p> <p><strong>(a) Inside the endpoint — the client watching its own traffic.</strong> This is the mailbox case. You are reading data that was <em>already, legitimately, delivered to you</em>. Watching your own browser’s responses is not a threat to anyone, because you are the rightful recipient. Opening your own mail is not interception in any adversarial sense at all.</p> <p><strong>(b) On-path — a third party wedged into the network middle.</strong> This is the famous <strong>Man-in-the-Middle (MITM) attack</strong>. Someone sits between client and server and reads or rewrites both sides of a conversation they were never part of. (The <code class="language-plaintext highlighter-rouge">mitm</code> in tools like <em>mitmproxy</em> is literally this acronym.)</p> <p>Here’s the subtlety that took me a second: the <em>tooling</em> for (a) and (b) overlaps almost entirely. The thing that flips one into the other is <strong>position and consent</strong>—am I standing inside my own endpoint looking at my own traffic, or have I inserted myself into someone else’s conversation without their agreement?</p> <blockquote> <p><strong>The Hidden Math — “location decides legality” is an equivalence-class statement.</strong> This felt fuzzy until I phrased it the way I’d phrase anything else. Take the set of all “look at the bytes” actions. Define a relation: two actions are equivalent if they’re <em>technically</em> identical (same <code class="language-plaintext highlighter-rouge">clone()</code>, same proxy, same packet read). Under that relation, debugging-my-own-traffic and MITM-ing-a-stranger land in the <em>same equivalence class</em>—the technique can’t tell them apart. So “is this okay?” simply <strong>is not a function of the technique</strong>; the technique is constant across the class. The thing that <em>does</em> vary—and therefore the only thing that can determine permissibility—is the pair (position, consent). When a question is invariant under the feature you were staring at, you’re staring at the wrong feature. That’s why “but it’s the same code” is never an answer to “was I allowed to.”</p> </blockquote> <p>So when I run interception against <em>my own VM</em>, I’m squarely in case (a). That’s not a loophole I’m exploiting; it’s the whole reason the exercise is clean.</p> <h2 id="the-technique-monkey-patching-fetch">The technique: monkey-patching <code class="language-plaintext highlighter-rouge">fetch</code></h2> <p>The most direct way to watch the conversation from inside the page is <strong>monkey-patching</strong>: replacing an existing function, at runtime, with a wrapper that preserves the original behavior but slips your code in around it.</p> <p><strong>Definition.</strong> In JavaScript, functions are <em>first-class values</em>—you can store one in a variable and reassign it. <code class="language-plaintext highlighter-rouge">window.fetch</code> is just a function value hanging off the global object. So you can grab the original, stash it under another name, and put your own wrapper in its place.</p> <p><strong>Intuition.</strong> It’s function composition. If the original is ( f ), you install</p> \[f'(x) = (\text{after} \circ f \circ \text{before})(x)\] <p>where <code class="language-plaintext highlighter-rouge">before</code> peeks at the arguments (the outgoing request) and <code class="language-plaintext highlighter-rouge">after</code> peeks at the result (the incoming response), while ( f ) itself runs untouched in the middle.</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// (1) Keep the original under a new name. Like g = f before we reassign f.</span>
<span class="kd">const</span> <span class="nx">originalFetch</span> <span class="o">=</span> <span class="nb">window</span><span class="p">.</span><span class="nx">fetch</span><span class="p">;</span>

<span class="c1">// (2) Put a wrapper where window.fetch used to be — this is the monkey-patch.</span>
<span class="nb">window</span><span class="p">.</span><span class="nx">fetch</span> <span class="o">=</span> <span class="k">async</span> <span class="nf">function </span><span class="p">(...</span><span class="nx">args</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="p">[</span><span class="nx">url</span><span class="p">,</span> <span class="nx">options</span><span class="p">]</span> <span class="o">=</span> <span class="nx">args</span><span class="p">;</span>
  <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">-&gt; request out:</span><span class="dl">'</span><span class="p">,</span> <span class="nx">url</span><span class="p">,</span> <span class="nx">options</span> <span class="o">||</span> <span class="dl">''</span><span class="p">);</span>   <span class="c1">// before</span>

  <span class="c1">// (3) Call the real fetch, passing every argument through untouched.</span>
  <span class="kd">const</span> <span class="nx">response</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">originalFetch</span><span class="p">.</span><span class="nf">apply</span><span class="p">(</span><span class="k">this</span><span class="p">,</span> <span class="nx">args</span><span class="p">);</span>

  <span class="c1">// (4) Clone before reading — the body is a single-use stream (see below).</span>
  <span class="kd">const</span> <span class="nx">clone</span> <span class="o">=</span> <span class="nx">response</span><span class="p">.</span><span class="nf">clone</span><span class="p">();</span>
  <span class="nx">clone</span><span class="p">.</span><span class="nf">json</span><span class="p">()</span>
    <span class="p">.</span><span class="nf">then</span><span class="p">(</span><span class="nx">data</span> <span class="o">=&gt;</span> <span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">&lt;- response JSON:</span><span class="dl">'</span><span class="p">,</span> <span class="nx">data</span><span class="p">))</span>   <span class="c1">// after</span>
    <span class="p">.</span><span class="k">catch</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span><span class="cm">/* not JSON, ignore */</span><span class="p">});</span>

  <span class="c1">// (5) Hand the untouched original back to the page. It never notices.</span>
  <span class="k">return</span> <span class="nx">response</span><span class="p">;</span>
<span class="p">};</span>
<span class="nx">console</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="dl">'</span><span class="s1">fetch interceptor installed.</span><span class="dl">'</span><span class="p">);</span>
</code></pre></div></div> <p>Paste that into the DevTools <strong>Console</strong> while sitting on your own page, run a search, and the raw JSON shows up in the console <em>before</em> it ever becomes a list item—including fields the page chose not to display. That’s the mail, opened on arrival.</p> <blockquote> <p><strong>The Hidden Math — why <code class="language-plaintext highlighter-rouge">clone()</code> is not optional.</strong> The <code class="language-plaintext highlighter-rouge">response.clone()</code> line looks like defensive boilerplate, but it’s forced by a real constraint: an HTTP response body is a <strong>stream</strong>, and reading it (<code class="language-plaintext highlighter-rouge">.json()</code>, <code class="language-plaintext highlighter-rouge">.text()</code>) <em>consumes</em> it. Model the body as a resource with a use-count of exactly one. The page also intends to spend that one read to render the results. If my interceptor reads first, the page’s read fails; if the page reads first, mine does. There is no interleaving that lets a single-use resource satisfy two independent consumers. <code class="language-plaintext highlighter-rouge">clone()</code> resolves it by producing a second, independent stream so each consumer gets its own. The deeper “why a stream at all?” is a memory bet: a response might be a multi-gigabyte video, and you’d rather process it as it flows than hold the whole thing in RAM. Single-use is the price of not assuming everything fits in memory.</p> </blockquote> <p>A second small but deliberate choice: in step (4) I used <code class="language-plaintext highlighter-rouge">.then(...)</code> instead of <code class="language-plaintext highlighter-rouge">await</code>. If I had <code class="language-plaintext highlighter-rouge">await</code>-ed the clone’s body before returning, the page would be stuck waiting for <em>my</em> snooping to finish before it got its response. Pushing the peek into a <code class="language-plaintext highlighter-rouge">.then</code> callback lets the real response return immediately and the observation happen alongside it. A passive interceptor should disturb the original flow as little as possible.</p> <h2 id="stateless-http-and-the-real-identity-of-the-cookie">Stateless HTTP, and the real identity of “the cookie”</h2> <p>This is the part I’d most wondered about, and it has a clean theoretical core.</p> <p><strong>Definition.</strong> HTTP is fundamentally <strong>stateless</strong>: the server treats every request as an independent, first-time stranger. Request A and request B are, by default, unrelated as far as the protocol is concerned.</p> <p><strong>Intuition.</strong> But “staying logged in” obviously exists—so state is being <em>layered on top</em> of a stateless protocol. The mechanism is sessions and cookies:</p> <ol> <li>You log in once; the server verifies you and issues a <strong>session token</strong>—a long random string.</li> <li>The browser stores it as a <strong>cookie</strong> and <em>automatically attaches it to the header of every subsequent request</em>.</li> <li>On each request the server reads the token and goes “ah, it’s you again,” reconstructing continuity it doesn’t natively have.</li> </ol> <p>So a session is a function from token to identity, evaluated fresh on every request, faking persistence over a protocol that has none.</p> <blockquote> <p><strong>The Hidden Math — a cookie is a memoized identity lookup.</strong> Re-deriving “who is this and are they authenticated?” from scratch on every request would mean re-running the whole login computation each time—an expensive pure function of your credentials. The session token is the cached key into a lookup table the server keeps: present the key, get the precomputed identity back in O(1) instead of recomputing it. Stateless HTTP plus a session store is just <strong>memoization</strong>—trading a bit of server-side memory to avoid recomputing an expensive result on every call. Which immediately tells you where the danger lives: in memoization the cache key <em>is</em> the credential. Anyone holding the key gets the cached answer, no questions asked. That’s why a leaked session token is as good as a password (this is <strong>session hijacking</strong>), and why cookie attributes like <code class="language-plaintext highlighter-rouge">HttpOnly</code>, <code class="language-plaintext highlighter-rouge">Secure</code>, and <code class="language-plaintext highlighter-rouge">SameSite</code> exist—they’re all about keeping the cache key from leaking to the wrong holder.</p> </blockquote> <p>Worth separating two ideas the token touches. <strong>Authentication</strong> answers “who are you?”—the token proves identity. <strong>Authorization</strong> answers “are you allowed to do this?”—a well-built API enforces it <em>separately</em>, so that even a valid token can’t reach data outside its permissions.</p> <h2 id="the-honest-correction-i-owe-this-post">The honest correction I owe this post</h2> <p>When I first poked at a real site’s background requests, I described it to myself as “checking the API through the developer tab.” That phrasing was wrong in a way worth fixing, because it blurs two very different things:</p> <ul> <li>a <strong>public, documented developer API</strong>—an endpoint a company <em>publishes and licenses</em> for programmatic use, with terms and stability guarantees; versus</li> <li>an <strong>observed internal endpoint</strong>—the private URL a site calls for its own frontend, which I happened to <em>watch</em> in the Network tab.</li> </ul> <p>They look similar in DevTools and behave nothing alike in practice. The internal one carries no promise of stability (it can change without notice and break everything built on it) and, on someone else’s site, automating against it with a reused login session is a different conversation entirely—about terms of service and consent, i.e. squarely case (b) territory. None of <em>that</em> is what this post is about. Everything here ran against my own VM, where I’m the rightful recipient of my own mail. The interesting structure survives completely intact without ever leaving case (a).</p> <h2 id="what-i-actually-want-to-build-from-here">What I actually want to build from here</h2> <p>The plan that’s forming isn’t “intercept more aggressively”—it’s to <strong>build the other side of the boundary and attack my own design.</strong> Concretely: add a real login endpoint to my mini app, issue a genuine session token, and make the API reject unauthenticated requests with a <code class="language-plaintext highlighter-rouge">401</code>. Then put on the analyst hat, pull my own token out of the cookie header, and reconstruct an authenticated request by hand from a script.</p> <p>The reason that’s the right next move: it closes the loop on this whole post. It turns the cookie from a mysterious string into a memoization key I issued myself; it makes the <code class="language-plaintext highlighter-rouge">401</code>/<code class="language-plaintext highlighter-rouge">200</code> boundary the <em>authentication</em> concept I can touch; and because it’s all my server, I can build a deliberately leaky version and a properly hardened one and diff them. The best way to understand a defense is to be the one who has to build it after watching how easily the undefended version gives up its mail.</p> <p>And there’s a satisfying symmetry to end on. I started by wanting to receive my own data the way I open my own mail. The natural finish is to become the post office for a moment—to issue the tokens, stamp the envelopes, and decide who gets to open what. You don’t really understand a mailbox until you’ve also had to run the mailroom.</p>]]></content><author><name></name></author><category term="security"/><category term="web"/><category term="xhr"/><category term="fetch"/><category term="web-security"/><category term="networking"/><category term="javascript"/><category term="monkey-patching"/><summary type="html"><![CDATA[Starting from a simple question—where does all this data come from?—a math student's tour through XHR/Fetch interception, the client-server trust boundary, stateless HTTP, and why location (not the tool) decides whether you're debugging or attacking.]]></summary></entry><entry><title type="html">Navigating the City with Math: From Navy Waves to Traffic Waves</title><link href="https://jac2125.github.io/blog/2026/STGMS-Review/" rel="alternate" type="text/html" title="Navigating the City with Math: From Navy Waves to Traffic Waves"/><published>2026-02-09T00:00:00+00:00</published><updated>2026-02-09T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2026/STGMS-Review</id><content type="html" xml:base="https://jac2125.github.io/blog/2026/STGMS-Review/"><![CDATA[<script>
window.MathJax = {
  tex: {
    inlineMath: [['$', '$'], ['\\(', '\\)']],
    displayMath: [['$$', '$$'], ['\\[', '\\]']],
    processEscapes: true
  }
};
</script> <style>.highlight-box,.highlight-box *{background-color:#f0f9ff;color:#0c4a6e!important}.highlight-box{border-left:4px solid #0ea5e9;padding:1rem;margin:1.5rem 0;border-radius:0 8px 8px 0}.critical-box,.critical-box *{background-color:#fff1f2;color:#881337!important}.critical-box{border-left:4px solid #e11d48;padding:1rem;margin:1.5rem 0;border-radius:0 8px 8px 0}.proposal-box,.proposal-box *{background-color:#f0fdf4;color:#14532d!important}.proposal-box{border-left:4px solid #16a34a;padding:1rem;margin:1.5rem 0;border-radius:0 8px 8px 0}.nuance-box,.nuance-box *{background-color:#fff7ed;color:#7c2d12!important}.nuance-box{border-left:4px solid #f97316;padding:1rem;margin:1.5rem 0;border-radius:0 8px 8px 0}.math-note,.math-note *{background-color:#fafafa;color:#525252!important}.math-note{border:1px solid #e5e7eb;padding:1rem;border-radius:.5rem;margin-top:1rem;font-size:.9em}.highlight-box svg path{fill:#0c4a6e!important}.critical-box svg path{fill:#881337!important}.proposal-box svg path{fill:#14532d!important}.nuance-box svg path{fill:#7c2d12!important}</style> <h2 id="1-intro-from-the-sea-to-the-streets">1. Intro: From the Sea to the Streets</h2> <p>I have been serving in the ROK Navy since June 2025. Sailing through the seas, my last seven months seem to have disappeared like the <strong>wake behind the ship</strong>, vanishing into the white foam. Now that I finally have some time to think about my future outside the military, I have found myself captivated by the problem of traffic prediction.</p> <p>When planning a trip, I was never sure exactly when to leave to arrive on time. Modern technologies like Google Maps offer real-time navigation and ETAs, but some might question: <em>can they really give us 100% accuracy?</em> The ETA fluctuates constantly as road conditions change. Can we achieve higher precision in estimating these flows?</p> <p>Today, I want to introduce the <strong>Spatio-Temporal Graph Neural Network with Multi-timeScale (STGMS)</strong>. This model predicts urban traffic flow using multi-timescale features and spatio-temporal dependencies. It utilizes the Laplacian Matrix to determine differences between adjacent nodes and employs an attention mechanism to detect global patterns on the graph \(G\), representing our complex urban traffic network.</p> <h2 id="2-core-theory-the-language-of-graphs">2. Core Theory: The Language of Graphs</h2> <h3 id="1-laplacian-matrix-defining-smoothness">1) Laplacian Matrix: Defining Smoothness</h3> <h4 id="definition">Definition</h4> <p>Let \(G = (V, E)\)be a simple undirected graph. The Laplacian matrix\(L\) is defined as: \(L = D - A\) where \(D\)is the degree matrix (diagonal) and\(A\) is the adjacency matrix. Consider a simple graph forming a “triangle” (\(K_3\)):</p> <div style="text-align: center; margin: 2rem 0;"> <img src="/assets/img/IMG_8751.jpeg" alt="Triangle Graph" style="width: 250px; filter: invert(0); transition: filter 0.3s;" class="dark:invert"/> <p style="font-style: italic; color: #666; font-size: 0.9rem;">Figure 1: A simple Triangle Graph</p> </div> <p>For this graph, the Laplacian matrix is: \(L = \begin{pmatrix} 2 &amp; -1 &amp; -1 \\ -1 &amp; 2 &amp; -1 \\ -1 &amp; -1 &amp; 2 \end{pmatrix}\)</p> <h4 id="smoothness-of-the-graph-signal">Smoothness of the Graph Signal</h4> <p>The Laplacian matrix has a fundamental property related to the “smoothness” of a signal defined on the graph. Let \(\mathbf{x} \in \mathbb{R}^N\) be a signal vector on the nodes (e.g., traffic speed at each intersection). Then, the quadratic form (Dirichlet Energy) is: \(\mathbf{x}^T L \mathbf{x} = \sum_{(i,j) \in E} (x_i - x_j)^2\)</p> <p>If this value is small, the signal \(\mathbf{x}\) changes smoothly across the graph—meaning neighbors have similar traffic conditions.</p> <div style="width: 100%; margin: 2rem 0; border: 1px solid #e2e8f0; border-radius: 8px; overflow: hidden; box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);"> <iframe src="/assets/html/graph-laplacian.html" width="100%" height="700px" frameborder="0" loading="lazy" style="display: block;" title="Graph Laplacian Explorer"></iframe> </div> <p style="text-align: center; color: #666; font-size: 0.9em; margin-top: 0.5rem;"><em>Tip: Click an empty spot to add nodes and connect them.</em></p> <div class="nuance-box"> <p><strong>Mathematical Nuance: The Symmetry Requirement</strong><br/><br/> There is a subtle mathematical contradiction here. Real-world traffic is <strong>directed</strong> (e.g., one-way streets, different flows for AM/PM). However, the definition \(L = D - A\) typically assumes an <strong>undirected</strong> graph (\(A\) is symmetric).<br/><br/> <strong>Why?</strong> The <em>Spectral Theorem</em> guarantees that a matrix has an orthonormal basis of eigenvectors (crucial for the Graph Fourier Transform) if and only if the matrix is symmetric. If we used a directed graph’s Laplacian, the eigenvalues could be complex numbers, breaking the physical interpretation of “frequencies.”<br/><br/> <strong>The Compromise:</strong> Therefore, models like STGMS often symmetrize the graph (\(A_{sym} = A + A^T\)) or use distance-based Gaussian kernels to ensure mathematical stability, effectively treating the road network as a “proximity map” rather than a flow map.</p> </div> <h3 id="2-the-spectral-bridge-from-fourier-to-graph-convolution">2) The Spectral Bridge: From Fourier to Graph Convolution</h3> <p>To understand why STGMS uses Chebyshev polynomials, we must first look at the <strong>Convolution Theorem</strong>. In classical signal processing, convolution in the time domain is multiplication in the frequency domain.</p> <p>For a graph signal \(\mathbf{x}\), the Graph Fourier Transform (GFT) is defined using the eigenvectors \(U\)of the Laplacian\(L\). The convolution of signal \(\mathbf{x}\)with a filter\(g_\theta\) is: \(\mathbf{x} *_G g_\theta = U g_\theta(\Lambda) U^T \mathbf{x}\) Here, \(\Lambda\) is the diagonal matrix of eigenvalues (frequencies).</p> <h2 id="3-why-chebnet-solving-the-on3-bottleneck">3. Why ChebNet? (Solving the \(O(N^3)\) Bottleneck)</h2> <h3 id="the-computational-hurdle">The Computational Hurdle</h3> <p>The formula above is mathematically elegant, but computationally expensive. To perform the eigendecomposition \(L = U \Lambda U^T\), we need <strong>\(O(N^3)\)</strong> operations.</p> <div class="highlight-box"> <p><strong>The Problem:</strong> For a city-wide traffic network with thousands of nodes (\(N\)), calculating \(U\)and\(U^T\)at every step is impossible for real-time prediction. We need a way to apply the filter\(g_\theta(\Lambda)\)without actually finding the eigenvectors\(U\).</p> </div> <h3 id="the-polynomial-shortcut">The Polynomial Shortcut</h3> <p>This is where polynomial approximation saves the day. If we approximate the filter function \(g_\theta(\Lambda)\)as a polynomial of eigenvalues, say\(P(\Lambda) = \sum_{k=0}^K \theta_k \Lambda^k\), a beautiful property of linear algebra emerges: \(U P(\Lambda) U^T = P( U \Lambda U^T ) = P(L)\)</p> <p>Suddenly, we don’t need the eigenvectors \(U\)anymore! We only need to compute powers of the Laplacian matrix\(L^k\). STGMS specifically uses <strong>Chebyshev polynomials</strong> for this approximation because they are orthogonal and numerically stable.</p> <p>According to the STGMS model configuration, the recurrence relation is defined as: \(Ch_i(L_G) = 2 L_G Ch_{i-1}(L_G) - Ch_{i-2}(L_G)\)</p> <p>with initial conditions: \(Ch_1(L_G) = L_G, \quad Ch_0(L_G) = 0\)</p> <div class="math-note"> <p><strong>Professor’s Observation: The \(Ch_0=0\) Assumption</strong><br/> In standard Approximation Theory, the 0-th order Chebyshev polynomial is typically \(T_0(x) = 1\)(Identity Matrix). However, STGMS defines it as\(0\).<br/><br/> <strong>Why?</strong> This is likely an architectural design choice for Deep Learning. STGMS employs <strong>Residual Connections</strong> (\(y = F(x) + x\)). Since the residual connection already adds the input \(x\)(effectively\(1 \cdot x\)) to the output, including an Identity term in the convolution (\(Ch_0\)) would be redundant. Setting \(Ch_0=0\) forces the GCN layer to learn “pure diffusion” (changes based on neighbors) while the residual path handles information preservation.</p> </div> <h2 id="4-stgms-deep-dive-the-recipe-for-prediction">4. STGMS Deep Dive: The Recipe for Prediction</h2> <p>We have our ingredients (Laplacian Matrix) and our tool (ChebNet). Now, let’s look at the recipe. The STGMS model processes data systematically, much like a chef preparing a complex dish: prepping (decomposition), cooking (blocks), and refining.</p> <h3 id="step-1-decomposition-prepping-the-data">Step 1: Decomposition (Prepping the Data)</h3> <p>Traffic data might look chaotic, but it follows patterns. STGMS decomposes raw data into:</p> <ul> <li><strong>Weekly Period:</strong> Traffic next Monday will likely resemble last Monday.</li> <li><strong>Daily Trend:</strong> The morning rush hour happens every day.</li> <li><strong>Residual (Recent):</strong> Sudden accidents or weather changes that deviate from the patterns.</li> </ul> <h3 id="step-2-spatio-temporal-block-mixing-space-and-time">Step 2: Spatio-Temporal Block (Mixing Space and Time)</h3> <p>This is the engine of the model. It combines three powerful mechanisms:</p> <h4 id="1-temporal-attention-time">1. Temporal Attention (Time)</h4> <p>“Does yesterday’s congestion matter right now?” Temporal attention assigns weights to different past time steps.</p> <h4 id="2-spatial-attention-global-context">2. Spatial Attention (Global Context)</h4> <p>“Why does a jam in the city center affect the outskirts?” Spatial attention looks for semantic relationships across the graph.</p> <h4 id="3-chebyshev-gcn-local-diffusion">3. Chebyshev GCN (Local Diffusion)</h4> <p>This handles the physical propagation of traffic. By using the polynomial approximation (\(L^k\)), it aggregates information from \(K\)-hop neighbors efficiently without expensive matrix inversions.</p> <div class="critical-box"> <p><strong>The “Over-smoothing” Problem</strong><br/><br/> Mathematically, as \(K\)increases (multiplying\(L\) many times), the node features in a GNN tend to converge to a stationary distribution. This is known as <strong>Over-smoothing</strong>.<br/><br/> If \(K\)is too large, the unique signal of each intersection is washed out, and every node starts looking the same. This is why models typically limit\(K\)(e.g.,\(K=3\))—it captures the local neighborhood without destroying the distinct identity of the nodes.</p> </div> <h2 id="5-future-work-from-flow-to-intention">5. Future Work: From Flow to Intention</h2> <p>Up to this point, we have analyzed how STGMS cooks the data using fluid dynamics analogies (\(x^T L x\)). But as I stand on the deck of my ship, a fundamental question strikes me:</p> <p><em>“Is it really okay to treat cars like water molecules?”</em></p> <h3 id="current-limitations-cars-are-not-raindrops">Current Limitations: Cars Are Not Raindrops</h3> <p>Most GNN-based traffic models assume that traffic flow is governed by physical proximity (topology). However, traffic jams often arise from the <strong>clashing of intentions</strong>—what I call “OD (Origin-Destination) based Turbulence.” Even if two roads are connected (\(A_{ij}=1\)), flow might stop if drivers are trying to merge aggressively to reach a destination.</p> <h3 id="proposal-from-topological-to-intentional-graphs">Proposal: From Topological to Intentional Graphs</h3> <p>Therefore, I propose a new direction: learning the <strong>Latent Destination Distribution</strong> to dynamically alter the graph structure.</p> <div class="proposal-box"> <h4>Proposal: Dynamic Intention Graph Learning</h4> <p>Instead of a static adjacency matrix $$A$$, we introduce a time-varying <strong>Intention Graph $$A^{(t)}_{intent}$$</strong>:</p> <p>\(A^{(t)}_{final} = A_{topo} + \alpha \cdot A^{(t)}_{intent}\)</p> <p>Here, $$A^{(t)}_{intent}$$ connects nodes based on current travel demands (OD patterns), not just physical proximity.</p> </div> <div class="nuance-box"> <p><strong>Professor’s Note: A Mathematical Challenge</strong><br/><br/> <strong>The Spectral Dilemma:</strong> Introducing a dynamic \(A^{(t)}_{intent}\) poses a fascinating mathematical challenge.<br/><br/> If \(A^{(t)}_{intent}\)represents “intention to move from A to B,” it is inherently <strong>asymmetric</strong>. As we discussed in Section 2, the Laplacian of an asymmetric matrix may yield <strong>complex eigenvalues</strong>. This breaks the foundation of the Chebyshev approximation, which relies on eigenvalues being real and bounded within\([-1, 1]\).<br/><br/> <strong>The Solution?</strong> To implement this “Intention Graph,” we would likely need to abandon Spectral GCNs (ChebNet) and adopt <strong>Spatial GNNs</strong> (like Graph Attention Networks or Message Passing Neural Networks). Spatial methods operate directly on the graph structure without relying on the eigendecomposition of the Laplacian, making them robust to the asymmetry of human intention.</p> </div> <p>Just as I learned to read the currents of the sea, I hope to one day read the currents of human intention flowing through the city. That is the research I aspire to pursue after my service concludes.</p>]]></content><author><name></name></author><category term="review"/><category term="math"/><category term="GNN"/><category term="Traffic Prediction"/><category term="Spectral Graph Theory"/><category term="STGMS"/><category term="Research"/><summary type="html"><![CDATA[A deep dive into STGMS and Spectral Graph Theory, connecting naval experiences to urban traffic flow.]]></summary></entry><entry><title type="html">KnapsackProblem</title><link href="https://jac2125.github.io/blog/2025/Knapsack-Problem/" rel="alternate" type="text/html" title="KnapsackProblem"/><published>2025-05-29T00:00:00+00:00</published><updated>2025-05-29T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2025/Knapsack-Problem</id><content type="html" xml:base="https://jac2125.github.io/blog/2025/Knapsack-Problem/"><![CDATA[<h1 id="knapsack-problem---intro">Knapsack Problem - Intro</h1> <p>While going through “The Design of Approximation Algorithm”, I encountered an interesting section: The Knapsack Problem. Well, from <strong>Combinatorial Optimization</strong> by Bernhard Korte &amp; Jens Vygen, the book introduces this problem as “easiest NP-hard problem” compared to the MINIMUM WEIGHT PERFECT MATCHING PROBLEM.</p> <h2 id="the-application-of-knapsack-problem">The Application of Knapsack Problem.</h2> <ol> <li>Cargo Loading &amp; Container Packing: A shipping company must pack a container or truck under weight/volume limits.</li> <li>Investment Portfolio Selection: An investor allocates a fixed budget among assets or projects to maximize expected return.</li> <li>Project &amp; Budget Selection (Capital Budgeting): A firm chooses which R\&amp;D or capital projects to fund within its available capital.</li> <li>Advertisement Slot Scheduling: An online platform fills limited ad slots (time or impressions) to maximize ad revenue.</li> <li>Cloud Computing Resource Allocation: A data center assigns virtual machines to servers under CPU/memory/energy constraints. –– Answered by ChatGPT</li> </ol> <h2 id="problem-simplified-version">Problem (Simplified Version)</h2> <p>A traveler with a knapsack comes across a treasure hoard. Unfortunately, his knapsack can hold only so much. What items should he place in his knapsack in order to maximize the value of the items he takes away?</p> <p><strong>Problem Definition</strong> Given a set of \(n\) items \(I = \{1,2,\dots,n\},\) where each item \(i\in I\) has</p> <ul> <li>weight \(w_i &gt; 0\)</li> <li>value \(v_i &gt; 0\)</li> </ul> <p>and given a knapsack capacity \(W &gt; 0\), find a subset \(S \subseteq I\) such that</p> \[\sum_{i \in S} w_i \;\le\; W\] <p>and the total value</p> \[\sum_{i \in S} v_i\] <p>is maximized.</p> <h2 id="dp-solution">DP Solution</h2> <p>This problem can be solved using Dynamic Programming by computing, for each prefix of items and each weight limit, the maximum achievable value.</p> <p>Define</p> \[f(i,w) \;=\; \max\Bigl\{\sum_{j\in S} v_j \;\big|\; S\subseteq\{1,\dots,i\},\;\sum_{j\in S} w_j \le w\Bigr\}.\] <p>Initialize</p> \[f(0,w) = 0 \quad\forall\,0\le w\le B.\] <p>Then for \(i=1,\dots,n\) and \(0\le w\le B\):</p> \[f(i,w) = \begin{cases} f(i-1,w), &amp; w_i &gt; w,\\ \displaystyle\max\bigl(f(i-1,w),\,f(i-1,w - w_i) + v_i\bigr), &amp; w_i \le w. \end{cases}\] <p>The optimal value is \(f(n,B)\). This runs in</p> \[O(nB).\] <p>Equivalently, one can do a value-based DP:</p> \[g(i,v) \;=\; \min\Bigl\{\sum_{j\in S} w_j \;\big|\; S\subseteq\{1,\dots,i\},\;\sum_{j\in S} v_j \ge v\Bigr\},\] <p>which yields running time</p> \[O(nV).\] <p>Hence overall time complexity is</p> \[O\bigl(n\min\{B,V\}\bigr).\] <h2 id="rounding-solution">Rounding Solution</h2> <p>To obtain a Fully Polynomial-Time Approximation Scheme (FPTAS), proceed as follows:</p> <ol> <li> <p>Let \(v_{\max} = \max_i v_i\) and choose</p> \[K = \frac{\varepsilon\,v_{\max}}{n}.\] </li> <li> <p>Define scaled values</p> \[v_i' = \left\lfloor \frac{v_i}{K} \right\rfloor \quad\forall i.\] </li> <li> <p>Run the value-based DP on \(v_i'\) instead of \(v_i\). This produces a solution whose total original value is at least \((1-\varepsilon)\) times optimum.</p> </li> </ol> <p>The running time is</p> \[O\Bigl(n\sum_{i=1}^n v_i'\Bigr) = O\Bigl(n\cdot \frac{\sum_i v_i}{K}\Bigr) = O\Bigl(\tfrac{n^2\,v_{\max}}{\varepsilon\,v_{\max}}\Bigr) = O\bigl(\tfrac{n^2}{\varepsilon}\bigr).\] <p>With more careful implementation one can achieve</p> \[O\Bigl(\tfrac{n^3}{\varepsilon}\Bigr)\] <p>or even</p> \[O\Bigl(\tfrac{n^2}{\varepsilon}\Bigr)\] <p>depending on how the DP is structured.</p> <p>I have made a Knapsack Calculator here:</p> <iframe src="/assets/webProject/Knapsack/index.html" width="100%" height="600" style="border:1px solid #ccc; border-radius:4px;" sandbox="allow-scripts allow-same-origin"> 당신의 브라우저가 iframe을 지원하지 않습니다. </iframe>]]></content><author><name></name></author><category term="math"/><category term="Knapsack"/><category term="math"/><category term="optimization"/><summary type="html"><![CDATA[Knapsack Problem - Intro]]></summary></entry><entry><title type="html">TraffifOptimization</title><link href="https://jac2125.github.io/blog/2025/Traffic-Optimization/" rel="alternate" type="text/html" title="TraffifOptimization"/><published>2025-05-18T00:00:00+00:00</published><updated>2025-05-18T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2025/Traffic-Optimization</id><content type="html" xml:base="https://jac2125.github.io/blog/2025/Traffic-Optimization/"><![CDATA[<h1 id="problem">Problem</h1> <p>One day I started to wonder how korean traffic lights are working. Usually, traffic light turns to red to let the passengers cross the road. But it alternates from red light to green light even when there is no passenger crossing the road, and the cars are waiting it to show them a pretty green circle.</p> <p>Assuming that we know the random variable \(X_r\), a number of cars passing the road \(r\), and a traffic function \(f(x)\) which tells how many seconds a car would take to pass the road based on the number of cars on the road, denoted by \(x\). <strong>These factors are not gonna be added at this point, I will start with a simplified version of this problem</strong></p> <p>What we need to do is to minimize the sum of each car’s arrival time. In other words, let \(c_i\) be the ith car, where \(i \in \{1,...,n\}\) and ach car \(c_i\) has arrival time \(t_i &gt; 0\). If time of arrival is \(0\), we treat that as trivial case as it does not have to move at all. Our goal is to minimize \(\Sum_{i = 0}^{n} t_i\)</p> <h2 id="intuition">Intuition</h2> <p>My first intuition tells me to build a graph and optimize the flow. Let \(G=(V,E)\) be a graph, where \(V\) is a node cars starting there journey and \(E\) be a road. Each car \(c_i\) has \(s_i\) and \(f_i\), starting node and finishing node respectfully. Also we are assuming there always exist a path from \(s_i\) to \(f_i$ for all\)i \in {1, …, n}$$</p> <p>Each edge \(e_j \in E\), where \(j \in {1, ..., m\}\) has a maximum capacity \(R_i \in \mathbb{N}\), and a weight \(w_i &gt; 0\) which tells how many miniutes/seconds a car takes to pass the road.</p> <p>Lastly, each node has a traffic light \(L_v\)</p> <p>we could construct a problem and apply an algorithm to solve it.</p> <h2 id="formalize-porblem">Formalize Porblem</h2> <p>I would like to start with a simplified version of this problem. Instead of letting cars start at multiple nodes, fix the starting node and the finshing node.</p>]]></content><author><name></name></author><category term="math"/><category term="math"/><category term="optimization"/><summary type="html"><![CDATA[Problem One day I started to wonder how korean traffic lights are working. Usually, traffic light turns to red to let the passengers cross the road. But it alternates from red light to green light even when there is no passenger crossing the road, and the cars are waiting it to show them a pretty green circle.]]></summary></entry><entry><title type="html">Unmasking the Cross Product: A Determinant’s Disguise</title><link href="https://jac2125.github.io/blog/2025/Thoughts-on-Cross-Product/" rel="alternate" type="text/html" title="Unmasking the Cross Product: A Determinant’s Disguise"/><published>2025-03-01T00:00:00+00:00</published><updated>2025-03-01T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2025/Thoughts-on-Cross-Product</id><content type="html" xml:base="https://jac2125.github.io/blog/2025/Thoughts-on-Cross-Product/"><![CDATA[<h3 id="the-high-school-mystery">The High School Mystery</h3> <p>Remember sitting in your high school math or physics class, and the teacher suddenly introduced the cross product? If you were anything like me, you probably just nodded along, memorized the weird criss-cross multiplication formula, and blindly accepted the rule: <em>“It creates a new vector perpendicular to the first two, and its length is the area of the parallelogram they form.”</em></p> <p>But seriously, <em>why</em>? How does multiplying and subtracting a bunch of coordinates magically spit out a perfectly perpendicular vector with such a specific length? We all memorized it without knowing what was actually happening underneath the hood.</p> <p>Well, after surviving linear algebra, I finally got my “Aha!” moment. Today, I want to delve into this mathematical magic trick and tell the real story of the cross product, using the elegant power of determinants and rotation matrices. Grab a cup of coffee, and let’s decode this!</p> <h3 id="my-aha-moment">My “Aha!” Moment</h3> <p>The turning point for me was realizing two beautiful things:</p> <p>First, one of the fundamental properties of a matrix determinant is that it measures <strong>area and volume</strong>. The messy cross product formula is essentially just calculating the area of a parallelogram spanned by two vectors and attaching it to a perpendicular direction.</p> <p>Second, the ultimate revelation: if you take two input vectors, say \(\mathbf{a}\) and \(\mathbf{b}\), rotate both of them using a rotation matrix \(R\), and <em>then</em> take their cross product, it is exactly the same as taking the cross product first and rotating the result! Mathematically, \((R\mathbf{a}) \times (R\mathbf{b}) = R(\mathbf{a} \times \mathbf{b})\). This single property is the secret key to proving why the cross product behaves the way it does.</p> <h3 id="the-formal-introduction">The Formal Introduction</h3> <p>Let’s start with the definition. Suppose we have two 3D vectors in real space: \(\mathbf{a} = \langle a_1, a_2, a_3 \rangle\) and \(\mathbf{b} = \langle b_1, b_2, b_3 \rangle\). The cross product is elegantly defined using the formal determinant of a 3x3 matrix:</p> \[\mathbf{a} \times \mathbf{b} = \begin{vmatrix} \mathbf{i} &amp; \mathbf{j} &amp; \mathbf{k} \\ a_1 &amp; a_2 &amp; a_3 \\ b_1 &amp; b_2 &amp; b_3 \\ \end{vmatrix}\] <p>When you expand this out, you get the classic, slightly intimidating formula:</p> \[\mathbf{a} \times \mathbf{b} = (a_2 b_3 - a_3 b_2)\mathbf{i} - (a_1 b_3 - a_3 b_1)\mathbf{j} + (a_1 b_2 - a_2 b_1)\mathbf{k}\] <p>The textbook promise is that this resulting vector has two incredible properties:</p> <ol> <li>It is completely <strong>perpendicular</strong> (orthogonal) to both \(\mathbf{a}\) and \(\mathbf{b}\).</li> <li>Its <strong>length</strong> is exactly equal to the area of the parallelogram formed by \(\mathbf{a}\) and \(\mathbf{b}\).</li> </ol> <h3 id="the-magic-of-determinants--volume">The Magic of Determinants = Volume</h3> <p>Before we prove anything, we need to agree on what a determinant actually <em>is</em>. It isn’t just a number you crunch for a test; it’s a geometric scaling factor. The determinant of a 2x2 matrix represents the <strong>area</strong> of the parallelogram formed by its column vectors, and a 3x3 determinant represents the <strong>volume</strong> of a parallelepiped.</p> <p>Why? Think about Elementary Row Operations (EROs). If you stretch one vector by scaling its x-component by a factor of 2, the total area (or volume) doubles. Predictably, the math matches the geometry: multiplying a row of a matrix by a scalar multiplies the entire determinant by that same scalar.</p> <p><em>(If you want to play around with interactive visual proofs of this, I highly recommend checking out this amazing resource from Georgia Tech’s Interactive Linear Algebra: <a href="https://textbooks.math.gatech.edu/ila/determinants-volumes.html">Determinants and Volumes</a>.)</em></p> <h3 id="lets-rotate-laying-the-vectors-flat">Let’s Rotate! Laying the Vectors Flat</h3> <p>To prove the cross product’s properties, we are going to use a brilliant trick. Analyzing vectors pointing in random 3D directions is hard. So, what if we just rotate the entire 3D space so that our vectors \(\mathbf{a}\) and \(\mathbf{b}\) lie perfectly flat on the xy-plane? If they are on the xy-plane, their z-components become exactly zero. Much easier to work with, right?</p> <p>To do this, we need a Rotation Matrix \(R\). We can build \(R = [\mathbf{u}_1, \mathbf{u}_2, \mathbf{u}_3]^T\) using the Gram-Schmidt process (a fancy term for “making things perpendicular to each other”):</p> <ul> <li>Let \(\mathbf{u}_1\) be the normalized unit vector pointing in the direction of \(\mathbf{a}\).</li> <li>Let \(\mathbf{w}_2 = \mathbf{b} - (\mathbf{b} \cdot \mathbf{u}_1)\mathbf{u}_1\). This strips away any part of \(\mathbf{b}\) that goes in the \(\mathbf{u}_1\) direction. Normalizing this gives us \(\mathbf{u}_2\).</li> <li>Let \(\mathbf{u}_3\) be a unit vector perpendicular to both \(\mathbf{u}_1\) and \(\mathbf{u}_2\) (we can easily find this by solving the system \(\mathbf{x} \cdot \mathbf{u}_1 = 0\) and \(\mathbf{x} \cdot \mathbf{u}_2 = 0\)).</li> </ul> <p>Because the rows of \(R\) are mutually orthogonal unit vectors, \(R\) is an orthogonal matrix, meaning \(R R^T = I\). Let’s check its determinant:</p> \[\det(R R^T) = \det(I) \implies \det(R)\det(R^T) = 1\] <p>Since \(\det(R) = \det(R^T)\), we get \(\det(R)^2 = 1\). By choosing the direction of \(\mathbf{u}_3\) properly (following the right-hand rule), we can ensure \(\det(R) = 1\), confirming it is a pure rotation without any weird reflections.</p> <p>Now, what happens when we apply this rotation to our vectors? Because \(\mathbf{a}\) is made up entirely of \(\mathbf{u}_1\) and has no \(\mathbf{u}_3\) component, the dot product \(\mathbf{u}_3 \cdot \mathbf{a} = 0\). The same goes for \(\mathbf{b}\). Therefore, our rotated vectors \(R\mathbf{a}\) and \(R\mathbf{b}\) will look like this:</p> <ul> <li> \[R\mathbf{a} = \langle x_1, y_1, 0 \rangle\] </li> <li> \[R\mathbf{b} = \langle x_2, y_2, 0 \rangle\] </li> </ul> <p>We successfully flattened them!</p> <h3 id="the-golden-equation">The Golden Equation</h3> <p>Now, I want to introduce you to a heavy-hitting theorem from linear algebra. For any invertible 3x3 matrix \(M\), there is a beautiful relationship with the cross product:</p> \[(M\mathbf{a}) \times (M\mathbf{b}) = (\det M) (M^{-T}) (\mathbf{a} \times \mathbf{b})\] <p>This equation looks scary, but let’s plug in our rotation matrix \(R\) instead of \(M\) and watch things simplify. We already proved that \(\det(R) = 1\). And since \(R\) is an orthogonal matrix, its inverse transpose is just itself! \(R^{-T} = (R^T)^T = R\).</p> <p>Substitute those in, and the beast becomes a beauty:</p> \[(R\mathbf{a}) \times (R\mathbf{b}) = R(\mathbf{a} \times \mathbf{b})\] <p><strong>What does this actually mean?</strong> It means that rotation <em>preserves</em> the cross product. If you calculate the cross product of the flattened vectors, it is exactly the same as taking the original cross product and just rotating it. Since rotation doesn’t change lengths or relative angles, if we can prove the properties for the flattened vectors, those properties must also be true for the original vectors!</p> <h3 id="the-punchline">The Punchline</h3> <p>Let’s finally calculate the cross product of our flattened vectors, \(R\mathbf{a} = \langle x_1, y_1, 0 \rangle\) and \(R\mathbf{b} = \langle x_2, y_2, 0 \rangle\):</p> \[(R\mathbf{a}) \times (R\mathbf{b}) = \begin{vmatrix} \mathbf{i} &amp; \mathbf{j} &amp; \mathbf{k} \\ x_1 &amp; y_1 &amp; 0 \\ x_2 &amp; y_2 &amp; 0 \\ \end{vmatrix}\] <p>Because the z-components are zeros, the \(\mathbf{i}\) and \(\mathbf{j}\) terms completely cancel out. We are left with only the \(\mathbf{k}\) component:</p> \[(R\mathbf{a}) \times (R\mathbf{b}) = (x_1 y_2 - x_2 y_1)\mathbf{k}\] <p>Look closely at that coefficient: \((x_1 y_2 - x_2 y_1)\). Does it look familiar? <strong>It is exactly the 2x2 determinant of the xy-coordinates!</strong> As we established earlier, a 2x2 determinant calculates the area of a parallelogram. So, the length of our new vector is exactly the area of the parallelogram formed by \(R\mathbf{a}\) and \(R\mathbf{b}\). And because it only has a \(\mathbf{k}\) component, it is pointing straight up along the z-axis, making it perfectly perpendicular to the xy-plane where our vectors live.</p> <p>Since the rotation matrix \(R\) preserves all lengths and angles, rotating everything back to the original orientation means the original cross product \(\mathbf{a} \times \mathbf{b}\) must <em>also</em> be perpendicular to the original vectors, and its length must be the exact area of the parallelogram. Proof complete!</p> <h3 id="wrapping-up">Wrapping Up</h3> <p>Let’s quickly recap our mathematical journey:</p> <ol> <li>We recognized that determinants naturally measure area and volume.</li> <li>We used Gram-Schmidt to invent a rotation matrix \(R\) that neatly placed our arbitrary vectors flat onto the xy-plane.</li> <li>We used the golden identity to show that rotating the inputs just rotates the cross product without changing its fundamental properties.</li> <li>We showed that on the xy-plane, the cross product perfectly spits out the 2x2 determinant (the area) purely in the perpendicular \(\mathbf{k}\) direction.</li> </ol> <p>The cross product isn’t just an arbitrary set of multiplications designed to torture students; it’s a perfectly logical consequence of how space and volume behave mathematically.</p> <p><strong>Fun Fact before you go:</strong> You might assume that you can do this cross product trick in any dimension, maybe 4D or 5D. Surprisingly, you can’t! The cross product (with these specific geometric properties) only exists in exactly <strong>3 dimensions and 7 dimensions</strong>. Sounds like science fiction, right? If you want to dive down a serious mathematical rabbit hole, check out the <a href="https://en.wikipedia.org/wiki/Seven-dimensional_cross_product">Seven-dimensional cross product on Wikipedia</a>.</p> <p>Until next time, keep questioning the formulas!</p>]]></content><author><name></name></author><category term="math"/><category term="math"/><category term="LinearAlgebra"/><summary type="html"><![CDATA[Diving deep into the geometric intuition and proofs behind the cross product using rotation matrices and determinants.]]></summary></entry><entry><title type="html">TSP(s)</title><link href="https://jac2125.github.io/blog/2025/TSP-Problems/" rel="alternate" type="text/html" title="TSP(s)"/><published>2025-02-25T00:00:00+00:00</published><updated>2025-02-25T00:00:00+00:00</updated><id>https://jac2125.github.io/blog/2025/TSP-Problems</id><content type="html" xml:base="https://jac2125.github.io/blog/2025/TSP-Problems/"><![CDATA[<h1 id="a-brief-intro-to-the-tsp">A Brief Intro to the TSP</h1> <p><em>A Gentle Intro Optimization</em> by B. Guenin, J. Konemann and L. Tuncel has introduced the TSP briefly. <br/> Consider a traveling salesman who needs to visit cities \(1, 2, 3, ..., n\) in some order and end up at the city where he started. Cost of travel from city \(i\) to city \(j\) is given by \(c_{ij}\). The goal is to find a <em>tour</em> of these \(n\) cities, visiting city exactly once, such that the total travel costs are minimized. \ <br/> The tour in this problem refers to a Hamiltonian cycle. And it is NP-complete to decide whether a given undirected graph \(G = (V, E)\) has a Hamiltonian Cycle.</p>]]></content><author><name></name></author><category term="math"/><category term="math"/><category term="TSP"/><summary type="html"><![CDATA[A Brief Intro to the TSP A Gentle Intro Optimization by B. Guenin, J. Konemann and L. Tuncel has introduced the TSP briefly. Consider a traveling salesman who needs to visit cities \(1, 2, 3, ..., n\) in some order and end up at the city where he started. Cost of travel from city \(i\) to city \(j\) is given by \(c_{ij}\). The goal is to find a tour of these \(n\) cities, visiting city exactly once, such that the total travel costs are minimized. \ The tour in this problem refers to a Hamiltonian cycle. And it is NP-complete to decide whether a given undirected graph \(G = (V, E)\) has a Hamiltonian Cycle.]]></summary></entry><entry><title type="html">Circulant Matrix + Eigenvectors/values</title><link href="https://jac2125.github.io/blog/2024/Circulant-Matrix-and-Eigenvectors&values/" rel="alternate" type="text/html" title="Circulant Matrix + Eigenvectors/values"/><published>2024-12-15T15:12:00+00:00</published><updated>2024-12-15T15:12:00+00:00</updated><id>https://jac2125.github.io/blog/2024/Circulant-Matrix-and-Eigenvectors&amp;values</id><content type="html" xml:base="https://jac2125.github.io/blog/2024/Circulant-Matrix-and-Eigenvectors&amp;values/"><![CDATA[<h1 id="1-problem">1. Problem</h1> <p>![[Pasted image 20241029213125.png]] Here is the proposition regarding the eigenvalues of Laplacian Matrix $C_n$ While reading the proof of this proposition, I have encoutered to a question of the following: ![[Pasted image 20241029213250.png]] the equation 4.6 to 4.8 is pretty straight forward, but it took me some time to understand this part: <strong>If $P$ is a cyclic permutation, then $P\vec{x}$ is also an eigenvector for $\lambda$. This means that $\vec{x}, P\vec{x},…,P^{n-1} \vec{x}$</strong> are all eigenvectors of $\lambda$ I did some of the research what the cyclic permutation is meaning and studying about topics related circulant matrix, which was also very interesting. So I wanted to write my own proof about this but it is not clear if this is really a “proof” (I am not sure if this proof can be a rigorous proof on this)</p> <p>Proposition: Let $\vec{x}$ be an eigenvector of $L_{C_n}$. If $P$ is a cyclic permutation, then $P\vec{x}$ is also an eigenvector for $\lambda$. This means that $\vec{x}, P\vec{x},…,P^{n-1} \vec{x}$</p> <p>The laplacian matrix of $C_n$ has the form \(\begin{bmatrix}2 &amp; -1 &amp; 0 &amp; ... &amp; -1 \\ -1 &amp; 2 &amp; -1 &amp; ... &amp; 0\\ \vdots &amp; &amp; \ddots &amp; &amp; \vdots\\ 0 &amp; ... &amp; -1 &amp; 2 &amp; -1\\ -1 &amp; ... &amp; 0 &amp; -1 &amp; 2\\ \end{bmatrix}\) Then</p> <p>I realized it: next we study <strong>fourier analysis</strong> to understand the rest of the proofs</p>]]></content><author><name></name></author><category term="math"/><category term="formatting"/><category term="math"/><summary type="html"><![CDATA[1. Problem ![[Pasted image 20241029213125.png]] Here is the proposition regarding the eigenvalues of Laplacian Matrix $C_n$ While reading the proof of this proposition, I have encoutered to a question of the following: ![[Pasted image 20241029213250.png]] the equation 4.6 to 4.8 is pretty straight forward, but it took me some time to understand this part: If $P$ is a cyclic permutation, then $P\vec{x}$ is also an eigenvector for $\lambda$. This means that $\vec{x}, P\vec{x},…,P^{n-1} \vec{x}$ are all eigenvectors of $\lambda$ I did some of the research what the cyclic permutation is meaning and studying about topics related circulant matrix, which was also very interesting. So I wanted to write my own proof about this but it is not clear if this is really a “proof” (I am not sure if this proof can be a rigorous proof on this)]]></summary></entry><entry><title type="html">Laplacian Matrix - Basic Properties</title><link href="https://jac2125.github.io/blog/2024/Laplacian-Matrix-(Basic-Properties)/" rel="alternate" type="text/html" title="Laplacian Matrix - Basic Properties"/><published>2024-11-10T15:12:00+00:00</published><updated>2024-11-10T15:12:00+00:00</updated><id>https://jac2125.github.io/blog/2024/Laplacian-Matrix-(Basic-Properties)</id><content type="html" xml:base="https://jac2125.github.io/blog/2024/Laplacian-Matrix-(Basic-Properties)/"><![CDATA[<p>This page is my study note of AN INTRODUCTION TO SPECTRAL GRAPH THEORY by Jiaqi Jiang</p> <p>From Wikipedia, it introduces the spectral theory as the following: In mathematics, spectral theory is an inclusive term for theories extending the eigenvector and eigenvalue theory of a single square matrix to a much broader theory of the structure of operators in a variety of mathematical spaces.</p> <p>Then Spectral Graph Theory is a study of Eigenvalues/Eigenvectors of Graph. It deals with matrix representations of graphs, such as Laplacian and Adjacency Matrix. In this page, I wrote some cool properties of Laplacian Matrix and will write further study note about it. Alright, before we hit the definition of Laplacian Matrix, here is the definition of \(L_{G_{\{u,v\}}}\).</p> <h2 id="definition-1">Definition 1</h2> <p>Suppose \(G = (V, E)\) is a graph with \(V = \{1, 2, \dots, n\}\). For an edge \(\{u, v\} \in E\), we define an \(n \times n\) matrix \(L_{G_{\{u,v\}}}\) by</p> \[l_{G_{(u,v)}}(i,j) = \begin{cases} 1 &amp; \text{if } i = j \text{ and } i \in \{u, v\}, \\ -1 &amp; \text{if } i = u \text{ and } j = v, \text{ or vice versa}, \\ 0 &amp; \text{otherwise}. \end{cases}\] <p>The matrix \(L_{G_{\{u,v\}}}\) has cool properties: \(\vec{x}^T L_{G_{\{u,v\}}} \vec{x} = (x_u - x_v)^2\) for all \(\vec{x} \in \mathbb{R}^n\)</p> <p>Then let’s define the laplacian matrix:</p> <h2 id="definition-2">Definition 2</h2> <p>For a graph \(G = (V, E)\), the Laplacian matrix \(L_G\) is defined as:</p> \[L_G = \sum_{\{u, v\} \in E} L_{G_{\{u, v\}}}\] <p>Alternative definition: Let \(G = (V, E)\) with \(n\) vertices \(v_1, ... v_n\), its Laplacian matrix \(L_{n\times n} is defined as\) L_{i,j}:= \begin{cases<em>} \text{deg}(v_i) &amp; \text{if } i=j, \ -1 &amp; \text{if } i \neq j \and (v_i, v_j) \in E \ 0 &amp; \text{otherwise} \end{cases</em>} This is equivalent to \(L = D - A\), where \(D\) is the degree matrix, and \(A\) is the graph’s adjacency matrix.</p> <p>We can check that this matrix is a real-symmetric matrix, where all the entries are real and symmetric. Here is one good fact about a real-symmetric matrix</p> <h2 id="theorem-1">Theorem 1</h2> <p>The eigenvalues of a self-adjoint matrix are all real.</p> <p><strong>Proof.</strong> Suppose \(\lambda\) is an eigenvalue of the self-adjoint matrix \(L\) and \(v\) is a nonzero eigenvector of \(\lambda\). Then,</p> <p>\(\begin{align*} \lambda \|v\|^2 &amp;= \lambda \langle v, v \rangle \\ &amp;= \langle \lambda v, v \rangle \\ &amp;= \langle Lv, \rangle \\ &amp;= \langle v, Lv \rangle \\ &amp;= \langle v, \lambda v \rangle &amp;= \bar{\lambda} \langle v, v \rangle &amp;= \bar{\lambda} \|v\|^2 \end{align*}\) Since \(\lambda = \bar{\lambda}\), \(\lambda\) is real.</p> <p>I just said “self-adjoint matrix”. A self-disjoint matrix \(M \in \mathbb{F}^{n\times n}\) is self-disjoint or Hemitian if the conjugate transpose is equal to the original matrix: \(M = M^*\) Since real-symmetric matrices are equal to its conjugate transpose, we can apply theorem number to this case as well.</p> <p>And then, this is the definition of positive-semidefinite matrix.</p> <h2 id="definition-34">Definition 3.4</h2> <p>An \(n \times n\) matrix \(M\) is called positive-semidefinite if \(\underline{x}^{T} M \underline{x} \geq 0\) for all \(\underline{x} \in \mathbb{R}^n\).</p> <h2 id="theorem-35">Theorem 3.5</h2> <p>For a graph \(G\), every eigenvalue of \(L_G\) is non-negative.</p> <p><strong>Proof.</strong> Suppose \(\lambda\) is an eigenvalue and \(\mathbf{x} \in \mathbb{R}^n\) is a nonzero eigenvector of \(\lambda\). Then,</p> \[\mathbf{x}^T L_G \mathbf{x} = \mathbf{x}^T (\lambda \mathbf{x}) = \lambda (\mathbf{x}^T \mathbf{x}).\] <p>Since \(\mathbf{x}^T L_G \mathbf{x} &gt; 0\) and \(\mathbf{x}^T \mathbf{x} &gt; 0\), we have \(\lambda &gt; 0\). \(\square\)</p> <p>As we have noted, the Laplacian matrix \(L_G\) is self-adjoint and consists of real entries. Thus, the Real Spectral Theorem states that \(L_G\) has an orthonormal basis consisting of eigenvectors of \(L_G\). Therefore, for a graph \(G\) of \(n\) vertices, we can find \(n\) eigenvalues (not necessarily distinct) for \(L_G\). We denote them as \(\lambda_1, \lambda_2, \ldots, \lambda_n\). Since they are all real and non-negative, we assume that</p> \[0 &lt; \lambda_1 \leq \lambda_2 \leq \cdots \leq \lambda_n.\] <p>Now we prove some fundamental facts about Laplacians. Recall that in the previous section we mentioned that the eigenvalues of the Laplacian tell us how connected a graph is. Now, we define connectedness. First, we give the definition of a <em>path</em>.</p> <h2 id="corollary-310">Corollary 3.10</h2> <p>Let \(G = (V, E)\) be a graph. Then the multiplicity of \(0\) as an eigenvalue of \(L_G\) equals the number of connected components of \(G\).</p> <p><strong>Proof.</strong> Suppose \(G_1 = (V_1, E_1), G_2 = (V_2, E_2), \ldots, G_k = (V_k, E_k)\) are the connected components of \(G\). Let \(\mathbf{w}_i\) be defined by</p> \[(w_i)_{j} = \begin{cases} 1 &amp; \text{if } j \in V_i, \\ 0 &amp; \text{otherwise}. \end{cases}\] <p>Then, it follows from the previous lemma that if \(\mathbf{x} \in \mathbb{R}^n\) is a non-zero eigenvector of \(0\), then \(x_i = x_j\) for any \(i, j \in V\) such that \(i, j\) are in the same connected component. So,</p> \[U_{i} = \text{Span}(\{\mathbf{w}_1, \mathbf{w}_2, \ldots, \mathbf{w}_k\}).\] <p>It is clear that \(\mathbf{w}_1, \mathbf{w}_2, \ldots, \mathbf{w}_k\) are linearly independent. Therefore, the multiplicity of \(0\) as an eigenvalue of \(L_G\) is the number of connected components in \(G\). \(\square\)</p> <h1 id="4-eigenvalues-and-eigenvectors-of-the-laplacians-of-some-fundamental-graphs">4. Eigenvalues and Eigenvectors of the Laplacians of Some Fundamental Graphs</h1> <p>Now we begin to examine the eigenvalues and the eigenvectors of the Laplacian of some fundamental graphs.</p> <h2 id="definition-41">Definition 4.1</h2> <p>A complete graph on \(n\) vertices, \(K_n\), is a graph \(G = (V, E)\) where \(V = \{1, 2, \ldots, n\}\) and \(E = \{(i, j) \mid i \neq j, i, j \in V\}\).</p> <h2 id="proposition-42">Proposition 4.2</h2> <p>The Laplacian of \(K_n\) has eigenvalue \(0\) with multiplicity \(1\) and eigenvalue \(\nu\) with multiplicity \(n - 1\).</p> <p><strong>Proof.</strong> The first part of the proposition simply follows from Corollary 3.9. To prove the second part of the proposition, consider the Laplacian of \(K_n\). It is an \(n \times n\) matrix defined by</p> \[a_{ij} = \begin{cases} -1 &amp; \text{if } i \neq j, \\ n - 1 &amp; \text{if } i = j. \end{cases}\] <p>Therefore, \(L_K - nI = M\) where \(M\) is the \(n \times n\) matrix with entries all equal to \(-1\). Clearly, \(M\) is not invertible and has rank \(1\). Thus \(n\) is an eigenvalue of \(L_K\). Then by Rank-nullity Theorem, \(\text{null}(M) = n - 1\). It follows that the eigenvalue \(\nu\) has multiplicity \(n - 1\). \(\square\)</p> <h2 id="definition-43">Definition 4.3</h2> <p>The path graph on \(n\) vertices, \(P_n\), is a graph \(G = (V, E)\) where \(V = \{1, 2, \ldots, n\}\) and \(E = \{(i, i + 1) \mid 1 \leq i &lt; n\}\).</p>]]></content><author><name></name></author><category term="math"/><category term="Spectral_Graph_Theory"/><summary type="html"><![CDATA[This page is my study note of AN INTRODUCTION TO SPECTRAL GRAPH THEORY by Jiaqi Jiang]]></summary></entry></feed>